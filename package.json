{
  "name": "daisyui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/daisyui && bun run build",
    "dev": "bun run build && cd packages/docs && bun run dev",
    "build:docs": "bun run build && cd packages/docs && bun run build",
    "play": "bun run build && cd packages/playground && bun run dev",
    "format": "bunx prettier 'packages/{daisyui,docs}/{src,functions}/**/*.{css,js,json,svelte}' --write",
    "lint": "bunx -y oxlint@latest",
    "test": "bun test",
    "wallace": "bunx -y wallace-cli packages/daisyui/daisyui.css",
    "publish:alpha": "cd packages/daisyui && bun publish --tag=alpha"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0-beta.3",
    "commit-and-tag-version": "12.5.0",
    "culori": "^4.0.1",
    "lightningcss": "^1.28.2",
    "postcss": "^8.4.49",
    "postcss-js": "^4.0.1",
    "postcss-selector-parser": "^7.0.0",
    "prettier": "^3.4.1",
    "prettier-plugin-svelte": "^3.3.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "tailwindcss": "^4.0.0-beta.3",
    "zlib": "^1.0.5"
  }
}
